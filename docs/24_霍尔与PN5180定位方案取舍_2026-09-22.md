# 霍尔传感器与 PN5180 定位方案取舍

当前主线采用 PN5180 逐格扫描；霍尔传感器只作为占位检测增强，不作为身份识别器件。PN5180 读取棋子底部 ISO15693 标签 UID，线圈编号提供棋格坐标，霍尔传感器加磁铁只判断有无棋子。

试验顺序：单线圈单标签；3×3 逐格 UID；记录空读、误读、邻格串读和耗时；需要时加入 3 个 DRV5032 做对照；确认明显改善后才扩展到 9 个。

相关连接说明：`docs/23_3x3器件连接关系与开发板判定_2026-09-22.md`。
